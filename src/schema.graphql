# A Dog is the four-legged best friend of man
type Dog {
  id: String!
  breed: String!
  displayImage: String
  images: [Image]
  subbreeds: [String]
}

type Image {
  url: String!
  id: String!
}

type Query {
  dogs: [Dog]
  dog(breed: String!): Dog
}
